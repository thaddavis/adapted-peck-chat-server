{
  "name": "socket.io-chat",
  "version": "0.0.0",
  "description": "A simple chat client using socket.io",
  "main": "index.js",
  "author": "Grant Timmerman",
  "private": true,
  "license": "BSD",
  "dependencies": {
    "express": "4.13.4",
    "redis": "2.8.0",
    "socket.io": "^4.0.0",
    "socket.io-redis": "^6.0.1"
  },
  "scripts": {
    "start": "node index.js",
    "start-local": "FRONT_END_HOSTNAME=http://localhost:3000 REDIS_HOST=localhost PORT=4000 node index.js",
    "start-local-debug": "DEBUG=socket* REDIS_HOST=localhost PORT=4000 node index.js"
  }
}
